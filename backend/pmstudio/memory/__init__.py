"""L4 记忆层（M13–M17）。

分工（CONTRACTS C12 / PRD）：

- **M13** 说"这一轮该带什么"——产出带 `priority` 的上下文块，**一个 token 都不裁**
- **M14** 项目简报：常驻注入的那一小块，用户可直接编辑
- **M15** 长对话管理：说过的话与滚动摘要，**决定带全量还是带摘要**
- **M16** 检索：历史的按需取用（"最近的全量"归 M15）
- **M17** 反馈与画像：把用户的取舍沉淀成候选记忆

裁剪不在这里（那是 L6 的 M24）。
"""
