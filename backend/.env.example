# 复制成 backend/.env，把真实密钥填进去；.env 已被 .gitignore 忽略，永远不要提交。
#
# 只有一处读它：HTTP 入口启动时（web_api/app.py 的 lifespan → web_api/env.py），
# 且**不覆盖已有的环境变量**（CI / 容器里 export 的值优先）；文件不存在不是错误。
#
# 变量名必须与注册中心里那条模型条目的 `api_key_env` 对上（当前是 seeds/model.default.json）。
# 条目里永远只写变量名，密钥本身不进仓库、也不进注册条目。

DASHSCOPE_API_KEY=sk-在这里填你的密钥

# 想跑真链路冒烟（tests/bootstrap/test_live_model_smoke.py）也可以填在下面——
# 那条用例会按"哪家有密钥"在运行时把默认模型条目换掉，不改 seeds/ 里的数据：
# DEEPSEEK_API_KEY=sk-...
